/** Moved to src/shared/namProjects.ts so the main process / preload can import the same
 * definitions. This re-export keeps existing `../types/namProjects` imports working. */
export * from '../../../shared/namProjects'
